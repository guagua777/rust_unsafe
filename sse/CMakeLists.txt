PROJECT(SSE)

SET (SRCLIST main.cpp)

MESSAGE(STATUS "THIS IS BINARY_DIR" ${SSE_BINARY_DIR})
MESSAGE(STATUS "THIS IS SOURCE_DIR" ${SSE_SOURCE_DIR})
MESSAGE(STATUS "THIS IS CMAKE SOURCE_DIR" ${CMAKE_SOURCE_DIR})

ADD_EXECUTABLE(sse ${SRCLIST})